"use client";

/**
 * @deprecated Use MomentHost / SeasonOpeningMoment — first official War Room Moment.
 * Kept so any stray import fails soft (renders nothing).
 */
export default function SeasonOpenWelcome() {
  return null;
}
