"use client";

/**
 * Legacy full-screen rules popup — retired.
 *
 * First-login coaching is the PlayerWalkthrough (picks coach) + quiet picks
 * banner. Full rules stay under More → How to play. This component stays
 * mounted as a no-op so older imports don't break.
 */

export default function RulesOnboardingModal() {
  return null;
}
