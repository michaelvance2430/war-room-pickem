"use client";

/**
 * REMOVED — Host Tip / first-time coaching scaffold.
 *
 * Desktop vs mobile divergence was device-local first-time flags showing this
 * card on one client and not the other for the same league. League state must
 * not depend on coaching localStorage.
 *
 * Host next-action lives entirely in HomeCommishMissionButton
 * (resolveCommishHomeMission) + HomeWeekHero. One reality everywhere.
 */
export default function CommishSetupBanner() {
  return null;
}
